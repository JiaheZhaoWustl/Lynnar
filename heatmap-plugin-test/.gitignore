# Node
*.log
*.log.*
node_modules

out/
dist/
# `code.js` is the compiled plugin entrypoint referenced by `manifest.json`.
# Keep it tracked so the plugin can be loaded without requiring a build step.
